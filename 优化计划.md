# MES-dome · 优化计划

> 全项目 Review 后整理的**待改进项**与**已完成归档**。  
> 核心业务（状态机 / 报工 Consumer / MQ 生产消费）仍按 §11.1 **手写**；AI 可协助 Review 与非核心框架。

**最后整理**：2026-07-05（D7 前端壳 + D9 状态机 + JWT 角色 List 修复）

---

## 0. 项目现状总览

### 0.1 模块完成度

| 模块 | 状态 | 说明 |
|------|------|------|
| 工程 / pom / sql | ✅ | SB 4.1 + MP + Redis + Redisson + JWT；**RocketMQ 仅 pom + yml 占位** |
| D3 框架 | 🟡 | `Result` + 全局异常 + `@Valid`；**Filter 已统一 Result JSON** |
| D3 CORS | 🟡 | `CorsConfig` 已有；生产域名 / `allowCredentials` 组合待收紧 |
| D5 JWT / 双令牌 | ✅ | login / refresh / logout；jti 黑名单；`AuthSessionService` |
| D6 ERP 推单 | 🟡 | `POST /api/erp/work-orders` + SET NX；Apifox / RBAC 待做 |
| D8 工单查询 | 🟡 | 分页列表 + 详情含工序；Apifox 自测待做 |
| D9 状态机 | ✅ | `WorkOrderStateMachine` + 7 条 JUnit；纯逻辑无 Mapper |
| D4/D22 RocketMQ | ⏸ | **暂不写 Java 配置**（未连 NameServer 易启动失败）；D22 报工前再加 |
| Vue 前端 | ✅ | D7 登录 + `/admin` / `/terminal` 壳；D12/D19 页面待做 |

### 0.2 已实现 API

| 方法 | 路径 | 模块 |
|------|------|------|
| POST | `/api/auth/login` | auth |
| POST | `/api/auth/refresh` | auth |
| POST | `/api/auth/logout` | auth |
| POST | `/api/erp/work-orders` | erp |
| GET | `/api/work-orders` | workorder |
| GET | `/api/work-orders/{id}` | workorder |
| GET | `/api/Test` | 调试（待删） |

### 0.3 已完成（近期）

- [x] D5 双令牌 + access jti 黑名单 + 循环依赖解耦  
- [x] D6 ERP 推单（事务 + Redis SET NX + `BusinessException`）  
- [x] D8 工单分页 / 详情（VO 与 Entity 字段对齐；`MybatisPlusInterceptor` 分页）  
- [x] D9 工单状态机（7 态跃迁表 + `transit` / `resolveAfterReport` + 7 条单测）  
- [x] D7 Vue 前端壳（登录页、Pinia 鉴权、双端 Layout、Vite 代理）  
- [x] JWT `List<String>` 角色统一 `AuthorityUtils.toAuthorities`  
- [x] D3 CORS 基础配置（`CorsConfig`）  
- [x] **Filter 鉴权失败统一 `Result` JSON**（`ResultResponseWriter`，替代 `sendError`）  
- [x] Filter 内校验账号 `enabled`  

### 0.4 RocketMQ 策略（已定）

> **当前**：`pom.xml` 保留 `rocketmq-client` 依赖；`application.yml` 仅保留 Topic/Group **命名占位**；**不写** `DefaultMQProducer` / `@RocketMQMessageListener` 等 Java 配置类。  
> **原因**：本地未启 NameServer 时，Producer/Consumer Bean 常在启动期连网失败。  
> **何时加**：D22 报工异步前，连上 RocketMQ 后再加配置类 + 连通性自测。

---

## 1. 高优先级（联调 / 面试前）

### 1.1 响应格式统一

- [x] `JwtAuthenticationFilter`：`ResultResponseWriter` 写 `{code,message,data}`  
- [ ] Spring Security 默认入口（未带 token 被 `authorizeHttpRequests` 拦截）是否也返回 `Result` — 可选加 `AuthenticationEntryPoint`  
- [ ] `GlobalExceptionHandler` 的 `Exception` 分支应 `log.error("...", e)` 打堆栈（当前只打固定文案）  

### 1.2 RBAC

- [ ] ERP 推单限制 `ROLE_PLANNER`（或 admin）  
- [ ] 工单查询是否仅 planner 可查（当前任意登录用户）  
- [ ] 多角色用户：`findRoleByUserID` 只返回一条，应改 `List<String>`（一人多角色）  

### 1.3 会话缓存

- [ ] `mes:user:{username}` 写 Redis 时加 TTL（`setValueWithExpire`）  
- [ ] 登录成功调 `authSessionService.getByUsername` **预热**缓存  
- [ ] 禁用 / 改角色后 `deleteKey`  
- [ ] Redis 反序列化 `LinkedHashMap` → 加强 `AuthSession` 类型转换  

### 1.4 refresh / logout 健壮性

- [ ] `refresh` 时校验用户仍存在且 `enabled=1`  
- [ ] logout 仅吊销 access 时（refresh 已失效）的策略是否放宽  

### 1.5 白名单维护

- [ ] `SecurityConfig` 与 `JwtAuthenticationFilter.isPermitAllPath` **两处重复**，新增路径易漏 — 抽常量或只信 Security 链  

---

## 2. 中优先级（D6/D8 收尾 + D9 前）

### 2.1 ERP 推单（D6）

- [ ] Apifox：推单 → 重复推单 → 查 DB / Redis  
- [ ] 入参 `operations` 内 `seq` 重复前置校验  
- [ ] 推单写 `integration_log`（可延至 D10/D26）  

### 2.2 工单查询（D8）

- [ ] Apifox：分页默认参数、按 `status` / `erpOrderNo` 筛选、详情 `operations` 顺序  
- [ ] 列表 VO 可选加 `statusLabel`（0→已下发）方便前端  
- [ ] 工单不存在：当前 `BusinessException` → 400；是否改 404  

### 2.3 CORS 生产化

- [x] 基础 `CorsConfig`  
- [ ] `allowedOriginPatterns("*")` + `allowCredentials(true)` 浏览器会拒 — 改为 `http://localhost:5173` 等明确域名  
- [ ] 补充 `PATCH` 方法（若后续 REST 用到）  

### 2.4 JwtUtil

- [ ] `verifyAccessToken` 与 `resolveAccessTokenRemainingMs` 合并，避免重复验签  

### 2.5 权限转换复用

- [ ] Filter 与 `UserDetailsServiceImpl` 中 `String role → GrantedAuthority` 抽共用方法  

---

## 3. 低优先级（结构 / 规范）

### 3.1 模块耦合

- [ ] `OperationTask extends OperationDto`（erp 包 DTO）— 建议 Entity 独立，DTO 仅 API 用  
- [ ] `WorkOrder` Entity 去掉不必要的 `@Builder` 混用（若未使用）  

### 3.2 调试代码清理

- [ ] 删除或保护 `auth/controller/Test.java`（`/api/Test`）  
- [ ] `CacheServiceImpl` 注入的 `RedissonClient` 若未使用可删  

### 3.3 包结构（可选）

- [ ] `security/` 包：`JwtUtil`、`JwtAuthenticationFilter` 从 `common` 迁出  

### 3.4 配置与安全

- [ ] 数据库密码勿长期明文提交（本地 override / 环境变量）  
- [ ] prod profile JWT secret 走环境变量  

### 3.5 RocketMQ（D22 前不做）

- [ ] ~~D4 Producer/Consumer 骨架~~ → **延至 D22**，与报工模块一起加  
- [x] yml 保留 Topic 命名占位 + 注释说明  

---

## 4. 已完成归档（勿重复修）

<details>
<summary>D5 双令牌 / D6 踩坑 / SQL 修正（点击展开）</summary>

**D5**：Access JWT + Refresh UUID；`RefreshTokenStore`；`AccessTokenBlacklist`；`AuthSessionService` Cache-Aside。

**D6 踩坑**：方法名不一致、lambda 缺泛型、dead code、Entity 当 DTO、status/erp_status 混用、Redisson 锁误用 → 已改 SET NX。

**SQL**：`findUserByUserId` 查 `sys_user`；`findRoleByUserID` JOIN `role_code`。

</details>

---

## 5. 自测清单

### 认证

```text
[x] POST /api/auth/login → 200 + token
[x] POST /api/auth/refresh / logout
[x] Filter 401 → Result JSON（非 HTML sendError）
[ ] GET /api/work-orders 无 token → {"code":401,"message":"token不能为空","data":null}
[ ] enabled=0 → 401 Result
```

### ERP（D6）

```text
[ ] 推单 + 重复推单幂等
[ ] 无 token → 401 Result JSON
```

### 工单（D8）

```text
[ ] GET /api/work-orders?pageNum=1&pageSize=10
[ ] GET /api/work-orders/{id} → operations 按 seq 升序
[ ] 不存在 id → 400/404
```

---

## 6. 建议实施顺序

| 顺序 | 任务 | 预估 |
|------|------|------|
| 0 | D6/D8 Apifox 自测 | 1h |
| 1 | 前后端登录联调（`npm run dev` + `admin/123456`） | 0.5h |
| 2 | ERP + 工单 RBAC | 1h |
| 3 | CORS 改明确 origin | 0.5h |
| 4 | `mes:user:` TTL + 登录预热 | 0.5h |
| 5 | D10 ERP 关单 + 状态映射 | 1 天 |
| 6 | **D22** RocketMQ 配置（连上 MQ 后再写） | 2h |
| 7 | D12 工单列表 + 详情页 | 1 天 |

---

## 7. 参考

- [40天开发计划.md](./40天开发计划.md)  
- [CLAUDE.md](./CLAUDE.md)  
- [MES-设计摘要.md](./MES-设计摘要.md)
